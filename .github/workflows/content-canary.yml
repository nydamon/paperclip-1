name: Content Canary

on:
  # Run after every deploy completes
  workflow_run:
    workflows: ["Deploy Vultr"]
    types: [completed]
  # Run on schedule every 6 hours
  schedule:
    - cron: "0 */6 * * *"
  # Manual trigger
  workflow_dispatch:

concurrency:
  group: content-canary
  cancel-in-progress: true

jobs:
  canary:
    runs-on: ubuntu-latest
    if: >-
      github.event_name != 'workflow_run' ||
      github.event.workflow_run.conclusion == 'success'
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          sparse-checkout: scripts/content-canary.mjs

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Run content canary
        id: canary
        env:
          CANARY_SITE_URL: "https://viracue.ai"
        run: |
          node scripts/content-canary.mjs | tee /tmp/canary-report.md
        continue-on-error: true

      - name: Upload report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: content-canary-report
          path: /tmp/canary-report.md
          retention-days: 30

      - name: Fail if regressions found
        if: steps.canary.outcome == 'failure'
        run: |
          echo "Content canary detected regressions — see report artifact"
          exit 1
