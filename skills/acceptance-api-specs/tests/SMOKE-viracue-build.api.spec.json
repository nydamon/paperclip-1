{
  "method": "GET",
  "url": "https://viracue.ai/__build.json",
  "expectedStatus": 200,
  "expectedResponseSchema": {
    "type": "object",
    "required": ["sha", "deployedAt"],
    "properties": {
      "sha": { "type": "string", "pattern": "^[a-f0-9]{7,40}$" },
      "deployedAt": { "type": "string", "format": "date-time" }
    }
  },
  "notBody": ["unknown", "ECONNREFUSED", "Internal server error"]
}
